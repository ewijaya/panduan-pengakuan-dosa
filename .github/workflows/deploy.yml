name: Deploy to Cloudflare Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - run: npm run build

      # `wrangler pages deploy dist` is a direct upload: it only picks up
      # Functions from <upload-dir>/functions. The sources live at the repo
      # root (conventional for Pages), so stage them into the build output.
      - name: Stage Pages Functions
        run: cp -r functions dist/functions

      # wrangler.toml is local-dev only (see the note in that file): if it is
      # visible here, Wrangler deploys its config over the dashboard's and
      # drops the /admin secrets. Remove it before uploading.
      - name: Drop local-dev wrangler config
        run: rm -f wrangler.toml wrangler.json wrangler.jsonc

      - name: Deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy dist --project-name=panduan-pengakuan-dosa --branch=main
